# **Лабораторна робота №3 “Кластеризація”**
# **Мета**
Ознайомитися з методами **кластеризації** (clustering) на прикладі реального набору музичних даних **Spotify Tracks Dataset**. Навчитись підбирати оптимальну кількість кластерів (k) за допомогою методу ліктя **(elbow)**, виконувати кластеризацію методом **K-Means**, знижувати розмірність простору ознак за допомогою **PCA** для візуалізації та аналізувати характеристики утворених кластерів.

# **Необхідні інструменти**
Python (рекомендація):
- `pandas`, `numpy` — для обробки даних;
- `scikit-learn` — для реалізації K-Means, StandardScaler, PCA;
- `matplotlib / seaborn / plotly` — для візуалізації;
- `plotly.express` — для інтерактивної візуалізації кластерів.

Результат — **Jupyter Notebook** (на GitHub) або **Google Colab / Kaggle Notebook**.

# **Джерело даних**
**Spotify Tracks Dataset**
- Сторінка: https://www.kaggle.com/datasets/zaheenhamidani/ultimate-spotify-tracks-db 

Це набір даних, який містить метадані й числові аудіо-характеристики пісень зі Spotify, такі як `danceability`, `energy`, `loudness`, `speechiness`, `acousticness`, `instrumentalness`, `liveness`, `valence`, `tempo`, `duration_ms`, `popularity`.

# **Хід роботи**
**1. Завантаження та підготовка даних**
- Завантажити набір даних.
- Вивести перші кілька рядків і статистики колонок.
- Виконати **стандартизацію** числових ознак.
- Для зменшення кількості точок у візуалізації залишити лише треки з **popularity ≥ 85**. (Решту треків можна використати для навчання моделі, але візуалізувати лише популярні.)
- Вивести розмірність таблиці після фільтрації.

**2. Підбір оптимальної кількості кластерів (k) — метод ліктя**
- Ініціалізувати модель `KMeans` із різною кількістю кластерів (наприклад k = 2…15).
- Для кожного k обчислити інерцію (суму квадратів відстаней до центрів кластерів).
- Побудувати графік залежності інерції від k.
- Визначити оптимальне k.

**3. Побудова кластеризації**
- Виконати кластеризацію з обраним значенням k.
- Додати отримані мітки кластерів до DataFrame як нову колонку `luster`.
- Вивести кількість треків у кожному кластері.

**4. Візуалізація кластерів у просторі головних компонент**
- Виконати **PCA** до двох/трьох компонент для візуалізації.
- Побудувати 2D/3D-графік з кольором точок за значенням `cluster`. Для покращення читабельності візуалізувати лише треки з **popularity ≥ 85**.


**5. Аналіз кластерів**
- Для кожного кластера обчислити статистики основних ознак.
- Побудувати таблицю середніх значень основних ознак або графік (heatmap).
- Інтерпретувати кластери, наприклад:
  1. кластер 0 — енергійні танцювальні треки з високим темпом;
  2. кластер 1 — спокійні акустичні композиції;
  3. кластер 2 — експериментальні інструментальні треки тощо.
